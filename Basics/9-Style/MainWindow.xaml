<Window x:Class="_9_Style.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_9_Style"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <Style x:Key="StdButton" TargetType="Button">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Control.Background" Value="AliceBlue"/>
            <Setter Property="Control.FontFamily" Value="Calibri" />
            <Setter Property="Control.FontWeight" Value="Bold" />
        </Style>
    </Window.Resources>

    <Grid>
        <Button Content="I'm stylish" Style="{StaticResource StdButton}" Margin="0,0,0,100" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <Button Content="Me Too" Style="{StaticResource StdButton}" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <Button Content="Not me" Margin="0,100,0,0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Window>
